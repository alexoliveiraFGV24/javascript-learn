## Abrindo um projeto e criando um ambiente virtual

```bash
mkdir <nome_do_projeto>
cd <nome_do_projeto>
npm init -y
npm install <nome_da_biblioteca>
npm install  # Usado para instalar as dependências do package.json
npm start    # Usado para rodar scripts do package.json
```